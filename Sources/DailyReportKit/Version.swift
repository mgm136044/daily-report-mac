/// The package version. Bumped at release time; also the single symbol the
/// smoke test uses to prove the module builds and links.
public enum DailyReport {
    public static let version = "0.1.0"
}
